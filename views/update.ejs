<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>Edit Book</title>
    <link rel="preconnect" href="https://fonts.gstatic.com" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto&family=Zen+Dots&display=swap" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto|Varela+Round" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" />
    <link rel="stylesheet" href="../../style3.css" />
  </head>
  <body>
    <!-- PUT: Edit Modal HTML -->
    <!-- <div id="editEmployeeModal" class="modal fade"> -->
    <div class="modal-dialog">
      <div class="modal-content">
        <form action="/book/<%= book._id %>?_method=PUT" method="POST">
          <div class="modal-header">
            <h4 class="modal-title">Edit This Book!</h4>
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          </div>
          <div class="modal-body modal-body-add">
            <div class="form-group">
              <label class="modal-label" for="title">Title:</label>
              <input type="text" id="title" name="title" class="form-control" value="<%= book.title %>" required />
            </div>
            <div class="form-group">
              <label class="modal-label" for="author">Author:</label>
              <input type="text" id="author" name="author" class="form-control" value="<%= book.author %>" required />
            </div>
            <div class="form-group">
              <label class="modal-label" for="publisher">publisher:</label>
              <input type="text" id="publisher" name="publisher" class="form-control" value="<%= book.publisher %>" required />
            </div>
            <div class="form-group">
              <label class="modal-label" for="publish_date">Publish Date:</label>
              <input type="Date" id="publish_date" name="publish_date" class="form-control" value="<%= book.publish_date %>" required />
            </div>
            <div class="form-group">
              <label class="modal-label" for="quantity">Qte:</label>
              <input type="number" id="quantity" name="quantity" class="form-control" value="<%= book.quantity %>" required />
            </div>
          </div>
          <div class="modal-footer modal-footer-add">
            <!-- <input type="submit" class="btn btn-info" value="Save" /> -->
            <button class="btn btn-outline-success btn-lg">Save <i class="fas fa-save"></i></button>
          </div>
        </form>
        <div class="modal-footer modal-footer-add">
          <form action="/home">
            <button class="btn btn-danger">Cancel <i class="fas fa-window-close"></i></button>
          </form>
          <!-- <a href="/home">Cancel</a> -->
        </div>
      </div>
    </div>
    <!-- </div> -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    <script src="../../script.js"></script>
    <script
      defer
      src="https://use.fontawesome.com/releases/v5.15.3/js/all.js"
      integrity="sha384-haqrlim99xjfMxRP6EWtafs0sB1WKcMdynwZleuUSwJR0mDeRYbhtY+KPMr+JL6f"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
